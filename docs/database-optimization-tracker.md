# Database Optimization Tracker — LMS

Source: [`docs/database-sql-review.md`](./database-sql-review.md). Every row starts at `Pending Discussion`. Move to `Approved` (or `Deferred` / `Wontfix`) only after explicit sign-off. No code, schema, migration, or config change has been made yet.

Legend
- **AFK** — Claude can implement mechanically once approved (no business judgement required).
- **HITL** — needs human-in-the-loop decision (touches business rules, PII, prod safety, or fetch semantics).

| ID | Issue | Area | Finding | Priority | AFK/HITL | Recommended Action | Discussion Needed | Status |
|---|---|---|---|---|---|---|---|---|
| F-01 | [#20](https://github.com/sid12701/lms/issues/20) | Security / PII | Plaintext PII at rest on `borrower` (`pan`, `aadhar_number`, `mobile`, `email`, `date_of_birth`, `bank_account_number`, `ifsc_code`, `address_*`, `account_holder_name`); plaintext `lsp.webhook_signing_secret`; plaintext JSON in `loan_disbursement_request_log.{request,response}_payload_json`. (See `backend/src/main/resources/db/migration/V6,V10,V23,V33,V19`.) | P0 | HITL | Pick a column-level encryption strategy (Vault / KMS / pgcrypto envelope). Plan a phased rollout: borrower PII → disbursement payloads → webhook secret. Must precede F-10 (jsonb) for affected columns. | Encryption tech choice; key custody & rotation story; impact on search (PAN/mobile lookup) and trigram indexes (V48); compliance scope. | Pending Discussion |
| F-02 | [#21](https://github.com/sid12701/lms/issues/21) | Data integrity | No `CHECK` constraints anywhere: status enums, monetary `>= 0`, tenure `> 0`, `min_principal <= max_principal`, PAN/Aadhaar/IFSC regex, `paid + outstanding = total`. All enforced only in app code. | P1 | HITL | Add CHECK constraints in two waves: (a) monetary/tenure/product invariants, (b) regex constraints after F-01 decision. | Confirm every writer is already clean; choose to enforce as `CHECK` or DB-side `DOMAIN`; rollout for backfills. | Pending Discussion |
| F-03 | [#22](https://github.com/sid12701/lms/issues/22) | Referential integrity | `webhook_event_outbox.loan_application_id` (added in `V58`) has no FK to `loan_application(id)`. | P1 | HITL | Add FK after a one-shot orphan scan and remediation. | Run an orphan inventory first; decide ON DELETE behaviour (RESTRICT vs SET NULL). | Pending Discussion |
| F-04 | [#23](https://github.com/sid12701/lms/issues/23) | ORM fetch | `Borrower.visibleLspIds` is `@ElementCollection(fetch=EAGER)` (`backend/src/main/java/com/bhawana/lms/domain/Borrower.java:27-30`). Every load of a Borrower triggers a SELECT on `borrower_lsp_access`. | P1 | HITL | Switch to `LAZY` + initialise via `@EntityGraph` / `JOIN FETCH` where needed. Audit every caller first. | Identify callers that read `visibleLspIds` post-detach; risk of `LazyInitializationException`. | Pending Discussion |
| F-05 | [#24](https://github.com/sid12701/lms/issues/24) | ORM fetch | `LoanAccount.loanApplication` is `@OneToOne(fetch=EAGER, optional=false)` (`LoanAccount.java:27-29`). Owning-side OneToOne can't truly lazy-load without bytecode enhancement. | P1 | HITL | Either keep EAGER and accept the join, or enable Hibernate bytecode enhancement + flip to LAZY. | Decide on bytecode enhancement (build-time agent). Callers audit. | Pending Discussion |
| F-06 | [#25](https://github.com/sid12701/lms/issues/25) | Pagination | `OpsAlertRepository.findAllByOrderByCreatedAtDesc()` is unbounded. Alerts table grows monotonically. | P1 | HITL | Add `Pageable` overload + page through the UI; keep a small `findTopN` for legacy callers if needed. | Decide retention/archival policy for `ops_alert` alongside pagination. | Pending Discussion |
| F-07 | [#26](https://github.com/sid12701/lms/issues/26) | Storage | `report_request.report_content TEXT` stores full report bodies in-row (`V30`). Grows with every export. | P1 | HITL | Offload to MinIO/S3 and replace `report_content` with `storage_key` + `media_type`. Keep small reports inline if convenient. | Pick object store; retention; signed-URL story; migration of existing rows. | Pending Discussion |
| F-08 | [#27](https://github.com/sid12701/lms/issues/27) | Indexing | Redundant indexes: `idx_loan_application_status`, `_lsp_id`, `_loan_product_id`, `_created_at` (V6+V34) superseded by V48 composites; `idx_loan_account_lsp_disbursed_at` (V36) prefix of V48; `idx_borrower_pan` (V41) duplicates `uk_borrower_pan` (V43); `loan_account_loan_application_id` (V34) duplicates the unique constraint's implicit index. | P2 | AFK | Single migration `DROP INDEX CONCURRENTLY IF EXISTS …`. Run EXPLAIN before/after on prod-shape data. | None — but confirm no in-flight query plans depend on the single-column variants. | Pending Discussion |
| F-09 | [#28](https://github.com/sid12701/lms/issues/28) | Schema | `loan_application_intake_audit.payload_json VARCHAR(4000)` (V8) — truncation risk for large intakes. | P2 | AFK | `ALTER COLUMN ... TYPE TEXT`. Online change in Postgres. | None. | Pending Discussion |
| F-10 | [#29](https://github.com/sid12701/lms/issues/29) | Schema / JSON | `*_json TEXT` everywhere: `webhook_event_outbox.payload_json`, `loan_disbursement_request_log.*_payload_json`, `loan_application_status_transition.rejection_reason_json`, `ops_alert.context_json`, `app_user_audit_event.{before,after}_state_json`, `api_client_audit_event.details_json`. | P2 | HITL | Convert to `jsonb` once a future query needs path/index access. Gated on F-01 for any column that may become ciphertext. | Need a query usecase; otherwise leave as TEXT. | Pending Discussion |
| F-11 | [#30](https://github.com/sid12701/lms/issues/30) | Query / index | `IgnoreCase` repo methods (`findByUsernameIgnoreCase`, `findByEmailIgnoreCase`, `findByPanIgnoreCase`) generate `UPPER(col)=UPPER(?)` — bypass unique B-trees. | P2 | HITL | Either drop `IgnoreCase` (values are already normalised on write) or add functional indexes `UPPER(...)`. | Confirm normalisation is exhaustive on every write path. | Pending Discussion |
| F-12 | [#31](https://github.com/sid12701/lms/issues/31) | Query | Derived-delete methods (`LoanRepaymentScheduleInstallmentRepository.deleteByLoanAccount_Id`, `LoanProductLspMappingRepository.deleteByLoanProduct_Id`) do SELECT-then-per-row DELETE without `@Modifying`. | P2 | HITL | Replace with `@Modifying @Query("delete from …")`. | Decide whether L2 cache invalidation or auditing was relying on the per-row trip. | Pending Discussion |
| F-13 | [#32](https://github.com/sid12701/lms/issues/32) | Indexing | `idx_loan_application_assignment_event_application_created_at` (V11) on a deprecated table (V53 retired the write path). | P2 | HITL | Drop the index once we verify zero live writes (table retained for forensics). | Run write-path audit on `loan_application_assignment_event` first. | Pending Discussion |
| F-14 | [#33](https://github.com/sid12701/lms/issues/33) | Indexing | `idx_loan_account_disbursed_at` defined twice with different semantics (V29 full, V34 `IF NOT EXISTS` partial). V34's partial was silently skipped. | P2 | HITL | If `WHERE disbursed_at IS NOT NULL` queries dominate, drop the V29 full index and recreate as partial. | Confirm typical predicate shape before flipping. | Pending Discussion |
| F-15 | [#34](https://github.com/sid12701/lms/issues/34) | Schema | Over-sized columns: `borrower.aadhar_number VARCHAR(16)` (Aadhaar is 12), `borrower.ifsc_code VARCHAR(32)` (IFSC is 11). | P3 | HITL | Tighten after F-02 regex CHECKs are in place. | Coordinate with F-01 — encrypted blobs may need wider columns. | Pending Discussion |
| F-16 | [#35](https://github.com/sid12701/lms/issues/35) | Schema | `webhook_event_delivery_attempt.request_event_type/_delivery_id/_timestamp/_signature` have sentinel defaults `'UNKNOWN'/'0'/'UNSIGNED'` from V27 backfill. | P3 | HITL | Audit live writers; if all populate real values, drop defaults to surface bugs loudly. | Audit completeness of `WebhookDeliveryClient` writes. | Pending Discussion |
| F-17 | [#36](https://github.com/sid12701/lms/issues/36) | Security (defence in depth) | `report_request` not RLS-enabled. CSV bodies may contain cross-LSP data; today gated only by app-layer `lsp_id` filter. | P3 | HITL | Add RLS policy mirroring other tenant tables. | Decide whether system admin reports (no `lsp_id`) need a bypass policy. | Pending Discussion |
| F-18 | [#37](https://github.com/sid12701/lms/issues/37) | Schema | `lsp.webhook_endpoint_url`, `webhook_event_types`, `webhook_signing_secret` embedded in `lsp` (V23). Limits to a single endpoint per LSP. | P3 | HITL | Extract to `lsp_webhook_subscription` only if multi-endpoint is on the roadmap. | Product roadmap input. | Pending Discussion |
| F-19 | [#38](https://github.com/sid12701/lms/issues/38) | Schema | `refresh_token.username VARCHAR(255)` (V47) denormalised vs FK to `app_user(id)`. | P3 | HITL | Defer until user-rename is supported; then switch to `app_user_id UUID REFERENCES app_user(id) ON DELETE CASCADE`. | Confirm no current rename path. | Pending Discussion |
| F-20 | [#39](https://github.com/sid12701/lms/issues/39) | Data integrity | `loan_repayment_schedule_installment` lacks CHECK on `paid_amount = paid_principal + paid_interest` and `paid_amount + outstanding_amount = installment_amount`. | P3 | HITL | Add as part of F-02 wave (a). | Sample existing rows for violations before applying. | Pending Discussion |
| F-21 | [#40](https://github.com/sid12701/lms/issues/40) | Schema (normalisation) | `loan_application_document_access_audit.document_types VARCHAR(500)` stores delimited list. | P3 | HITL | Normalise to a child table if document-type analytics is on the roadmap. | Analytics needs first. | Pending Discussion |
| F-22 | [#41](https://github.com/sid12701/lms/issues/41) | Migration safety | V41/V43 backfills (borrower clone, then canonical merge) assume no concurrent writes. Large-data re-runs need a window. | P3 | HITL | Document the production-safety contract for any re-run; rehearse on staging before any cutover. | Whether re-run is even contemplated. | Pending Discussion |

