# Local profile: imports repo-root .env (run from backend/ or set env vars in the shell).

spring:
  config:
    import: optional:file:../.env[.properties]
  datasource:
    url: ${LMS_DB_URL}
    username: ${LMS_DB_USERNAME}
    password: ${LMS_DB_PASSWORD}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 600000
      keepalive-time: 60000
  flyway:
    enabled: true
    locations: classpath:db/migration
    placeholders:
      tenant_app_password: ${APP_TENANT_DATASOURCE_PASSWORD:lms_tenant_app_password}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: ${LMS_REDIS_HOST:localhost}
      port: ${LMS_REDIS_PORT:6379}
  mail:
    host: ${LMS_MAIL_HOST:localhost}
    port: ${LMS_MAIL_PORT:1025}
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

app:
  rate-limit:
    # Off by default for local: avoids a hard Redis dependency at startup.
    # Set APP_RATE_LIMIT_ENABLED=true when infra/redis is running (docker compose).
    enabled: ${APP_RATE_LIMIT_ENABLED:false}
  alert-rules:
    # Local seed and UI testing intentionally exercises repeated authentication.
    # Keep portfolio alerts enabled without auto-locking the bootstrap operator.
    auth-brute-force-threshold: ${APP_ALERT_RULES_AUTH_BRUTE_FORCE_THRESHOLD:1000}
    auth-brute-force-distributed-threshold: ${APP_ALERT_RULES_AUTH_BRUTE_FORCE_DISTRIBUTED_THRESHOLD:1000}
  security:
    bootstrap-user:
      # Explicit local-only example identities. These defaults apply only when the local
      # profile is explicitly active; no other profile inherits them.
      username: ${APP_SECURITY_BOOTSTRAP_USERNAME:ops.admin}
      email: ${APP_SECURITY_BOOTSTRAP_EMAIL:ops.admin@bhawana.local}
      password: ${APP_SECURITY_BOOTSTRAP_PASSWORD:ChangeMe123!}
      roles:
        - SYSTEM_ADMIN
        - OPS_USER
    jwt:
      secret: ${APP_SECURITY_JWT_SECRET:local-dev-jwt-secret-at-least-32-characters}
      secure-cookies: false
    cors:
      # Explicit local-only SPA dev-server origins (Vite on 5173; 4200 kept for legacy
      # tooling). These defaults apply only when the local profile is explicitly active.
      allowed-origins: ${APP_SECURITY_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:4200,http://127.0.0.1:4200}
  seed:
    sample-data:
      enabled: false
    demo-portfolio:
      enabled: false
  storage:
    documents:
      provider: ${APP_STORAGE_DOCUMENTS_PROVIDER:R2}
      root-path: ${APP_STORAGE_DOCUMENTS_ROOT_PATH:${java.io.tmpdir}/lms-documents-local}
      r2:
        endpoint: ${APP_STORAGE_DOCUMENTS_R2_ENDPOINT:}
        access-key: ${APP_STORAGE_DOCUMENTS_R2_ACCESS_KEY:}
        secret-key: ${APP_STORAGE_DOCUMENTS_R2_SECRET_KEY:}
        bucket: ${APP_STORAGE_DOCUMENTS_R2_BUCKET:}
        region: ${APP_STORAGE_DOCUMENTS_R2_REGION:auto}
    reports:
      r2:
        endpoint: ${APP_STORAGE_REPORTS_R2_ENDPOINT:${APP_STORAGE_DOCUMENTS_R2_ENDPOINT:}}
        access-key: ${APP_STORAGE_REPORTS_R2_ACCESS_KEY:${APP_STORAGE_DOCUMENTS_R2_ACCESS_KEY:}}
        secret-key: ${APP_STORAGE_REPORTS_R2_SECRET_KEY:${APP_STORAGE_DOCUMENTS_R2_SECRET_KEY:}}
        bucket: ${APP_STORAGE_REPORTS_R2_BUCKET:${APP_STORAGE_DOCUMENTS_R2_BUCKET:}}
        region: ${APP_STORAGE_REPORTS_R2_REGION:${APP_STORAGE_DOCUMENTS_R2_REGION:auto}}
  datasource:
    tenant:
      username: ${APP_TENANT_DATASOURCE_USERNAME:lms_tenant_app}
      password: ${APP_TENANT_DATASOURCE_PASSWORD:lms_tenant_app_password}
      connection-strategy: ${APP_TENANT_CONNECTION_STRATEGY:DIRECT_LOGIN}
