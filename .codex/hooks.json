{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f graphify-out/graph.json ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"graphify: A knowledge graph exists for this repo. Use graphify-out/GRAPH_REPORT.md and graph-backed context as the primary repo map before broad raw-file searching. Use raw files mainly for targeted verification after the graph narrows the area.\"}}' || true"
          }
        ]
      }
    ]
  }
}
